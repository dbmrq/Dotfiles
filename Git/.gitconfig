# =============================================================================
# Full Git Configuration
# =============================================================================
#
# Extends essential config with personal info and Mac-specific settings.
# Machine-specific settings (GitHub accounts, URL rewrites, user overrides)
# are in ~/.gitconfig.local, generated by the bootstrap script.
#
# =============================================================================

# Include essential settings (aliases, core options)
[include]
    path = ~/.gitconfig-essential

[user]
    name = Daniel M.
    email = danielbmarques@gmail.com

# --- Machine-specific Overrides ---
# Contains GitHub account URL rewrites and local user settings
[include]
    path = ~/.gitconfig.local

[core]
    excludesfile = ~/.gitignore

[github]
    user = dbmrq

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[credential]
    helper = osxkeychain
