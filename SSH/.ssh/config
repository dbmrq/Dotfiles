# SSH Configuration for Multiple GitHub Accounts
#
# Supports 3 GitHub accounts via host aliases:
#   - github-personal   -> dbmrq (personal)
#   - github-work       -> danatwex (work)
#   - github-enterprise -> W508153_wexinc (enterprise SSO)
#
# URL rewrites in .gitconfig automatically select the right host alias:
#   - dbmrq/* repos     -> github-personal
#   - danatwex/* repos  -> github-work
#   - wexinc/* repos    -> github-work
#   - other repos       -> default (set in ~/.ssh/config.local)
#
# SSH keys must be generated separately on each machine:
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_personal
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_work
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_enterprise
#
# Then add the public keys to each GitHub account's SSH settings.
# For enterprise SSO, authorize the key at: https://github.com/settings/keys

# --- Include machine-specific config (defines default github.com) ---
Include ~/.ssh/config.local

# --- Personal GitHub (dbmrq) ---
Host github-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_personal
    IdentitiesOnly yes

# --- Work GitHub (danatwex) ---
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_work
    IdentitiesOnly yes

# --- Enterprise GitHub (W508153_wexinc via SSO) ---
Host github-enterprise
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_enterprise
    IdentitiesOnly yes

# --- General SSH settings ---
Host *
    AddKeysToAgent yes
    UseKeychain yes

