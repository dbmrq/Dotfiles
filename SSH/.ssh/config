# SSH Configuration for Multiple GitHub Accounts
#
# This config uses host aliases to manage multiple GitHub identities.
# When cloning, use the appropriate host alias:
#
#   Personal (dbmrq):
#     git clone git@github-personal:dbmrq/repo.git
#
#   Work (danatwex):
#     git clone git@github-work:danatwex/repo.git
#
#   Enterprise (W508153_wexinc):
#     git clone git@github-enterprise:org/repo.git
#
# To convert an existing repo, update the remote:
#   git remote set-url origin git@github-personal:dbmrq/Dotfiles.git
#
# SSH keys must be generated separately on each machine:
#   ssh-keygen -t ed25519 -C "your-email" -f ~/.ssh/id_ed25519_github_personal
#   ssh-keygen -t ed25519 -C "your-email" -f ~/.ssh/id_ed25519_github_work
#   ssh-keygen -t ed25519 -C "your-email" -f ~/.ssh/id_ed25519_github_enterprise
#
# Then add the public keys to each GitHub account's SSH settings.
# For enterprise SSO, authorize the key at: https://github.com/settings/keys

# --- Personal GitHub (dbmrq) ---
Host github-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_personal
    IdentitiesOnly yes

# --- Work GitHub (danatwex) ---
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_work
    IdentitiesOnly yes

# --- Enterprise GitHub (W508153_wexinc via SSO) ---
Host github-enterprise
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_enterprise
    IdentitiesOnly yes

# --- Default GitHub (fallback) ---
# Uses the first available key; useful for public repos
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_personal
    IdentityFile ~/.ssh/id_ed25519_github_work
    IdentityFile ~/.ssh/id_ed25519_github_enterprise
    IdentitiesOnly yes

# --- General SSH settings ---
Host *
    AddKeysToAgent yes
    UseKeychain yes

