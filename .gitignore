# Vim
Vim/.vim/bundle
Vim/.vim/undo/*
.netrwhist

# Bootstrap state
Bootstrap/.bootstrap_state

# Machine-specific configs (generated by bootstrap)
*.local
.gitconfig.local
SSH/.ssh/config.local

# SSH keys and known hosts (should never be committed)
*.pub
id_*
*.pem
SSH/.ssh/known_hosts*

# GitHub CLI (contains auth tokens and user preferences)
Vim/.config/gh/

# uv (Python package manager - machine-specific)
Vim/.config/uv/
Vim/.config/fish/

# macOS
.DS_Store

# Project-local agent skills (installed via npx skills from .skills-manifest.txt)
# These are symlinks/copies created by npx skills for each agent
.agents/
.augment/
.cursor/
.claude/
.copilot/
