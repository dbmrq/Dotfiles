# SSH Configuration for Multiple GitHub Accounts
#
# This config uses host aliases to manage multiple GitHub identities.
#
# DEFAULT: Enterprise account (W508153_wexinc via SSO)
#   - Any github.com repo not matching dbmrq/ or danatwex/ uses enterprise
#   - Just clone normally: git clone git@github.com:org/repo.git
#
# EXCEPTIONS (handled via URL rewrites in .gitconfig):
#   - dbmrq/* repos -> automatically use github-personal
#   - danatwex/* repos -> automatically use github-work
#
# SSH keys must be generated separately on each machine:
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_personal
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_work
#   ssh-keygen -t ed25519 -C "email" -f ~/.ssh/id_ed25519_github_enterprise
#
# Then add the public keys to each GitHub account's SSH settings.
# For enterprise SSO, authorize the key at: https://github.com/settings/keys

# --- Default GitHub (Enterprise) ---
# Most repos on this machine are enterprise, so make it the default
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_enterprise
    IdentitiesOnly yes

# --- Personal GitHub (dbmrq) ---
# Used via URL rewrite for github.com:dbmrq/* repos
Host github-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_personal
    IdentitiesOnly yes

# --- Work GitHub (danatwex) ---
# Used via URL rewrite for github.com:danatwex/* repos
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github_work
    IdentitiesOnly yes

# --- General SSH settings ---
Host *
    AddKeysToAgent yes
    UseKeychain yes

