-- Colorscheme configuration
-- Use terminal colors (from Ghostty/terminal theme) instead of a vim colorscheme
-- This lets the terminal's theme control the colors
--
-- To use terminal colors:
-- 1. Disable termguicolors (done in options.lua)
-- 2. Don't set a colorscheme, or use 'default'
--
-- The terminal (Ghostty) sets the theme, vim just uses those colors.
return {}
